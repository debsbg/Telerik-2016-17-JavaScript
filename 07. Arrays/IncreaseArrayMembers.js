function solve(args) {
    var n = +args[0];
    var arr = [];
    var i, len;
    arr.length = n;
   // var arr1 = arr[n];
for (i = 0, len = 5*n; i < len; i+=5) {
    console.log(i);
    //i +=5;
}

}